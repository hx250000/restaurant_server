spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_dish?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  cloud:
    nacos:
      discovery:
        enabled: false