server:
  port: 8081

spring:
  application:
    name: user_service

  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_user?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false